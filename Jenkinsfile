pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Build Project"
            }
        }
        stage('Archive') {
            steps {
                echo "Archive Project"
            }
        }
        stage('Publish JUnit') {
            steps {
                echo "Publish JUnit"
            }
        }
        stage('Publish HTML') {
            steps {
                echo "Publish HTML"
            }
        }
    }
}
